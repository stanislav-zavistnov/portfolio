const a2swiper = new Swiper('.swiper-three', {
  // Optional parameters
  direction: 'horizontal',
  loop: true,
  slidesPerView: 1,
  spaceBetween: 200,

  // If we need pagination
  pagination: {
    el: '.swiper-pagination-three',
  },
});
